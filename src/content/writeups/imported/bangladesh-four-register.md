---
title: "Four Register — Field Notes"
description: "Artifact: fourregisters, stripped x86-64 PIE ELF, GCC 16.1.1 (2026), no source/service supplied. Program is an interactive 4-register VM. It uppercases input lines and accepts commands MOVI, ADD, XOR, ROL, S…"
published: "2026-07-20"
updated: "2026-07-20"
event: "bangladesh"
category: "AI"
kind: "field-note"
status: "solved"
tags: ["AI", "bangladesh", "Field notes"]
readingTime: 1
wordCount: 163
featured: false
sourcePath: "~/ctf/bangladesh/four_register/notes.md"
---

## Confirmed state

- Artifact: `four_registers`, stripped x86-64 PIE ELF, GCC 16.1.1 (2026), no source/service supplied.
- Program is an interactive 4-register VM. It uppercases input lines and accepts commands `MOVI`, `ADD`, `XOR`, `ROL`, `SWAP`, `MIX`, and `RUN`.
- Lines are tokenized on space/tab; each non-RUN instruction is encoded as 3 bytes and max 14 instructions are accepted. `RUN` requires exactly 14 prior instructions.
- Register initial state from `.rodata` 0x2220: `[0x13579bdf, 0x2468ace0, 0x0badf00d, 0xc001d00d]`.
- Operation bytecodes: MOVI `0x13`, ADD `0x27`, XOR `0x39`, ROL `0x4b`, SWAP `0x5d`, MIX `0x6f`.
- Required final registers: R0 `0xd0f7f5a4`, R1 `0x71d63782`, R2 `0x2c458dac`, R3 `0x8c64de6c`; final carry/flags word is `0x3f01` (carry=1 and all six operation types used). Another state/hash check is `0x0dd32fb3`.
- Success path prints 49 generated bytes; no plaintext flag string is stored.

## Working hypothesis / next tests

- Recover the 14-instruction program with a bit-vector constraint solver, then emulate the success-side output generator to recover `BDSEC{...}`.
- Need precisely model the common trace/hash code around 0x1450-0x1909 and understand the `ecx == 0x0dd32fb3` check.
