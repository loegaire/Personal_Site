---
title: "Rev Chibile — Field Notes"
description: "I just created a mini anime knowledge game, and I wanted to share it with you. Can you give it a try?"
published: "2026-06-28"
updated: "2026-06-28"
event: "sekai2026"
category: "Reverse Engineering"
kind: "field-note"
status: "reference"
tags: ["Reverse Engineering", "sekai2026", "Field notes"]
readingTime: 3
wordCount: 530
featured: false
sourcePath: "~/ctf/sekai2026/rev_chibile/notes.md"
---

Chibile

Reverse Engineering

Author: ELJoOker
500

HARD
Description

I just created a mini anime knowledge game, and I wanted to share it with you. Can you give it a try? 

solve the challenge and get the flag. 

## 2026-06-28 continuation

- Existing summary says local reverse recovered endpoint `8875cf0267a7c5510933aebf9de827b9.chals.sekai.team:1337`, build string `CHIBILE-R3-20260615`, baked half-keys, and phase-1 decrypt formulas.
- Workspace initially contains `rev_chibile.tar.gz`, `chibile_phase1_solver.py`, and this notes file only.
- `getent hosts 8875cf0267a7c5510933aebf9de827b9.chals.sekai.team` now resolves to `136.69.52.86`; previous DNS failure may be fixed.
- `nc -vz 8875cf0267a7c5510933aebf9de827b9.chals.sekai.team 1337` connects successfully; server sends no banner.
- Archive extracted under `artifacts/rev_chibile/` while preserving original `rev_chibile.tar.gz`.
- `PROTOCOL.md` only documents inner phase-1 secret derivation, not transport framing.
- `chibile.exe` strings include JSON templates:
  - `{"phase":1,"nonce":"%s","um_attest":"%s","km_attest":"%s","pid":%lu,"ts":%llu,"client_build":"%s"}`
  - `{"phase":2,"nonce":"%s","sid":"%s","typed_um":"%s","typed_km":"%s","ts":%llu,"client_build":"%s"}`
- Relevant markers in `chibile.exe`: `CBL2`, `CBR2`, `CBM1`, `CBF2`, `RSA1`; likely custom framed/encrypted transport.
- Current hypothesis: implement enough of the client transport to send phase 1 and phase 2 directly, without running the PE.
- Reverse progress:
  - Phase-1 function: `fcn.140009930`.
  - Phase-2 function: `fcn.14000a320`.
  - Shared transport: `fcn.14000a760`.
  - Transport sends little-endian 4-byte length, then a `CBL2` frame; receives little-endian 4-byte length, then a `CBR2` frame.
  - `CBR2` response body layout appears to be: magic 4, IV 16, ciphertext `len-0x34`, HMAC/tag 32.
  - Successful phase-2 returns a decrypted `CBF2` blob; client strips first 4 bytes and treats remaining bytes as PNG.
  - `CBL2` request likely contains magic 4, RSA-OAEP encrypted 112-byte key bundle (256 bytes), IV 16, ciphertext, HMAC/tag 32.
- Next: implement helpers matching `fcn.140009540` (AES), `fcn.1400097f0` (MAC), and `fcn.14000afd0` (pre/post transform).
- Confirmed helper behavior:
  - `fcn.140009540`: AES-CBC via BCrypt, 32-byte key, 16-byte IV, PKCS padding.
  - `fcn.1400097f0`: HMAC-SHA256(key, data).
  - `fcn.14000afd0`: in-place XOR stream; for each 32-byte block, keystream is `HMAC-SHA256(mask_key, nonce16 || u64le(block_index))`.
  - Transport uses a fresh phase nonce generated by phase-1/phase-2; nonce is hex-encoded into JSON, copied raw into request frame at offset 4, and used by the XOR stream.
  - RSA public key material is in `.rdata` at `0x140011190`: exponent `010001`, modulus begins `d460ce918e9898c8...`.
- Remaining uncertainty: exact generation/validation requirements for `um_attest` and `km_attest`.
- User provided a full Windows VM reachable as `ssh win11`.
- SSH probes:
  - `ssh win11 hostname` -> `DESKTOP-HR7QOE6`
  - `ssh win11 whoami` -> `desktop-hr7qoe6\thinh`
- Plan: copy the original challenge directory and current solver artifacts to the VM, then run/instrument the stock client in a real Windows environment to dump phase-1 or reproduce the exact transport.
- Windows VM results:
  - `ssh win11 hostname` -> `DESKTOP-HR7QOE6`; `whoami` -> `desktop-hr7qoe6\thinh`.
  - Remote account is elevated admin (`net session` succeeded).
  - Copied artifacts to `C:\Users\thinh\Desktop\rev_chibile\`.
  - `C:\Users\thinh\Desktop\rev_chibile\rev_chibile` contains the original challenge files.
  - Created kernel service `EacShield` with `binPath=%USERPROFILE%\Desktop\rev_chibile\rev_chibile\eac_shield.sys`.
  - `sc start EacShield` failed with Win32 577: Windows cannot verify the digital signature.
  - Host-side `virsh -c qemu:///system` is available. VM XML was backed up to `scratch/win11.secboot.xml`, then redefined with non-secure OVMF (`OVMF_CODE_4M.qcow2`, secure-boot/enrolled-keys disabled).
  - After a power cycle, `bcdedit /set testsigning on` succeeded and the VM was rebooted.
  - `sc start EacShield` now succeeds; `sc query EacShield` reports `STATE: RUNNING`.
  - No `cl` or `gcc` found in the guest PATH; use PowerShell `Add-Type`, Python/ctypes, or local cross-compiled helpers for Windows-side harnesses.
  - Next test: use the loaded driver plus `eac_nocrt.dll`/stock client to obtain valid `um_attest` and `km_attest`, then fetch/decrypt phase-1.
